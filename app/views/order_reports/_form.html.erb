<%= form_for(order_report) do |f| %>
  <% if order_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_report.errors.count, "error") %> prohibited this order_report from being saved:</h2>

      <ul>
      <% order_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :date_time %>
    <%= f.datetime_select :date_time %>
  </div>

  <div class="field">
    <%= f.label :order_id %>
    <%= f.text_field :order_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
